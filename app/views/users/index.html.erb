<div class="container row">
  <div class="index-left col-xs-12 col-md-8 ">
  <h1><%= link_to "siteworkrs :#{@users.count}", users_path %></h1>
  <ul>
    <% @users.each do |user| %>
    <li>
      <div class="message user-card">

        <%= cl_image_tag(user.photo, class: "avatar-medium") %>
        <div class="message-name">
          <h3><%= link_to user.name.capitalize, user %></h3>
        </div>
        <div class="message-body">
          <p><strong><p class="fas fa-clipboard-check">Skills: <%= user.skill %></p></strong></p>
          <p>Certificates: <%= user.certificates %></p>
        </div>
        <div class="message-status">
          <!-- <p class="status pending">Pending</p> -->
          <p class="price">£<%= user.rate %> per day</p>
        </div>
      </div>

    </li>
    <% end %>
  </ul>
  </div>
  <div class="index-right col-xs-12 col-md-4">

  </div>
</div>

